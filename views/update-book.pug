<!DOCTYPE html>
html
    head
        meta(charset='utf-8')
        meta(http-equiv='x-ua-compatible', content='ie=edge')
        meta(name='viewport', content='width=device-width, initial-scale=1.0')
        link(rel="stylesheet" href="/stylesheets/style.css")
    body
        h1= book.title
        form(method="post" action="/update/" + book.id + "?_method=PUT" onsubmit= "return confirm('Are you sure you want to update this book?')" enctype="application/x-www-form-urlencoded")
            input(type="hidden" name="_method" value="PUT")
            h2(class="error")= message
            ul(class="error")
                li= bookTitle
                li= author
            p
                label(for="title") Title
                input(name="title" type="text" id="title" value=book.title)
            p
                label(for="author") Author
                input(name="author" type="text" id="author" value=book.author)
            p
                label(for="genre") Genre
                input(name="genre" type="text" id="genre" value=book.genre)
            p
                label(for="year") Year
                input(name="year" type="text" id="year" value=book.year)
            p
                input(type="submit" value="Update Book")
        form(method="post" action="/update/" + book.id + "?_method=DELETE" onsubmit= "return confirm('Do you really want to delete this book?')")
            input(type="hidden" name="_method" value="DELETE")
            p
                a(class="button" href="/books") Cancel
            p
                input(type="submit" value="Delete Book")